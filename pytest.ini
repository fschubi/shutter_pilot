[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -q
# Ohne diesen Eintrag findet ein blank aufgerufenes `pytest` das Paket
# custom_components nicht – nur `python -m pytest` legt das cwd auf sys.path.
pythonpath = .
